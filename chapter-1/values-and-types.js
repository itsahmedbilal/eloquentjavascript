/**
 * Values and Types
 * JavaScript uses different types to represent various kinds of data.
 * Numbers: Integers and floating-point numbers (e.g., 5, 3.14).
 * Strings: Text data enclosed in quotes ("hello", 'world').
 * Booleans: True or false values (true, false).
 * Undefined: A value automatically assigned to variables that have been declared but not initialized.
 * Null: Represents "nothing" or "empty" but explicitly assigned.
 * */


let a = 1, b = 3.14, c = 'Hell World', d = true, e , f = null;

console.log(a, b, c, d, e, f);